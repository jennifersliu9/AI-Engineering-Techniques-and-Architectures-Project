# Runtime — pin versions so local, eval, and deploy stay aligned.
python-dotenv==1.1.1
numpy==2.2.6
scikit-learn==1.6.1
beautifulsoup4==4.13.4
lxml==5.4.0
pypdf==5.4.0
fastapi==0.115.12
starlette==0.46.2
uvicorn==0.34.2
faiss-cpu==1.9.0.post1
fastembed==0.6.1

# Optional LLM answers. Imported only when HARBORLINE_ANSWER_MODE=llm.
openai==1.82.0

# MCP stdio server (Cursor still has to enable MCP in Settings).
# Pin Starlette/SSE so FastAPI 0.115 and mcp 1.9 stay compatible.
sse-starlette==2.2.1
mcp==1.9.4
